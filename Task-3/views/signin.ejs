<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign In | Express</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        html, body {
            height: 100%;
            margin: 0;
        }

        body {
            display: flex;
            flex-direction: column;
            background: linear-gradient(135deg, #e0f7fa, #f1f8e9);
            overflow-x: hidden;
        }

        main {
            flex: 1;
        }

        :root {
            --bs-body-font-family: 'consolas', sans-serif;
        }

        .fade-in {
            animation: fadeIn 1s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(10px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        header {
            transition: transform 0.5s ease;
        }

        header:hover {
            transform: scale(1.02);
            transition: all 0.5s ease;
        }

        .card {
            background: linear-gradient(-45deg, #e4e4e4, #f1f8e9);
            transition: transform 0.5s ease;
            border: 1px solid #7c7a7a !important;
        }

        .card:hover {
            transform: scale(1.02);
            transition: all 0.5s ease;
        }

        input {
            background-color: transparent !important;
            border: 1px solid #7c7a7a !important;
        }

        input.form-control:focus,
        input.form-control:hover {
            box-shadow: 0 0 0 0.05rem #7c7a7a;
            transition: box-shadow 0.2s;
        }
    </style>
</head>

<body>
    <!-- Header Section -->
    <header class="bg-dark text-white text-center py-3 mb-4">
        <h1 class="fade-in">User Login Portal</h1>
        <p class="fade-in">Responsive Design with Bootstrap & Animations</p>
    </header>

    <!-- Main Content -->
    <main>
        <div class="container">
            <div class="row justify-content-center">
                <!-- Form Section -->
                <div class="col-lg-4 m-2">
                    <div class="card border rounded-4 p-4 shadow-lg fade-in">
                        <h3 class="mb-3">Login</h3>
                        <form action="/dashboard" method="POST" onsubmit="return validateForm()">
                            <div class="mb-3">
                                <label class="form-label mx-1 my-1">Email<sup class="text-danger fw-bold">*</sup></label>
                                <input type="email" name="email" style="text-transform: lowercase;" class="form-control"
                                    minlength="6" maxlength="50" required>
                            </div>
                            <div class="mb-3">
                                <label class="form-label mx-1 my-1">Password<sup class="text-danger fw-bold">*</sup></label>
                                <input type="password" id="password" name="password" class="form-control" required>
                            </div>
                            <button type="submit" class="btn btn-dark border rounded-3 py-2 mt-3 w-100">Submit</button>
                        </form>
                    </div>
                </div>

                <!-- Info Section -->
                <div class="col-lg-6 m-2">
                    <div class="card border rounded-4 p-4 mb-4 shadow-lg fade-in">
                        <h3>About This Form</h3>
                        <p>
                            This form showcases a modern, responsive design built with Bootstrap’s grid system.
                            It combines clean layout principles, subtle animations, and user-friendly interactions
                            to deliver a seamless experience across all devices.
                        </p>
                        <ul>
                            <li>Smooth, professional hover and transition effects</li>
                            <li>Consistent, accessible, and visually appealing interface</li>
                            <li>Fully responsive — optimized for desktops, tablets, and mobile devices</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer Section -->
    <footer class="bg-secondary text-white text-center p-2 mt-4">
        <div class="container">
            <p class="mb-2">&copy; 2025 Express. All rights reserved.</p>
            <div class="mb-2">
                <a href="tel:+916354223991" class="text-white text-decoration-none">+91 63542 23991</a> |
                <a href="mailto:rumiriyaz9286@gmail.com" class="text-white text-decoration-none">rumiriyaz9286@gmail.com</a> |
                <a href="https://www.linkedin.com/in/riyazrumi" target="_blank"
                    class="text-white me-3 text-decoration-none">LinkedIn</a>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // Optional: form validation logic
        function validateForm() {
            // Example: could add custom validation here
            return true;
        }
    </script>
</body>
</html>
