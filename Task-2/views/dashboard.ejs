<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | Express</title>
    <style>
          /* Button style */
          .new-btn {
            display: inline-block;
            background-color: #197738;
            color: white;
            padding: 0.6rem 1.2rem;
            border-radius: 6px;
            text-decoration: none;
            font-size: 0.95rem;
            font-weight: bold;
            transition: background 0.2s ease;
            margin-bottom: 1rem;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 0.95rem;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
            overflow: hidden;
        }

        thead {
            background: #426f9c;
            color: white;
            text-align: center;
        }

        th, td {
            padding: 0.75rem 1rem;
        }
        th {
            text-align: center;
        }td {
            text-align: left;
        }
        tbody tr {
            background: white;
            transition: background 0.2s ease;
        }
        
        tbody tr:nth-child(even) {
            background: whitesmoke;
        }

        tbody tr:hover {
            background: #e8f0fe;
        }
    </style>
</head>

<body style="margin: 0; background: linear-gradient(135deg, #eef2f7, #d6e4f0); display: flex; flex-direction: column; align-items: center; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;min-height: 100vh; padding: 1rem;">
    <a href="/signup" class="new-btn">+ New Credential</a>
    <div id="container"
        style="background: rgb(250, 242, 242); border-radius: 12px; width: 100%; max-width: 500px; padding: 2rem 2.5rem; box-shadow: 0 4px 25px rgb(62, 96, 115);">

        <h1
            style="margin-bottom: 1.5rem; margin-top: 0; font-size: x-large; font-weight: bold; text-align: center; color: #2c3e50;">✨ Welcome to Express JS! ✨</h1>
        <h2 style="font-size: large; font-weight: bold; text-align: center; color: #2c3e50;">All Members</h2>
        
        <table>
            <thead>
                <tr>
                    <th>Username</th>
                    <th>Email</th>
                </tr>
            </thead>
            <tbody>
                <% for (let email in credentials) { %>
                    <tr>
                        <td data-label="Username"><%= credentials[email].username %></td>
                        <td data-label="Email"><%= credentials[email].email %></td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>

</html>
